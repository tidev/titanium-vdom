import { TitaniumElementRegistry } from './TitaniumElementRegistry';

export function registerTitaniumElements(registry: TitaniumElementRegistry): void {
    // Empty dummy function to make the titanium-vue-template-compiler happy. It's built
    // using rollup which is not able to load platform specific files.
}
